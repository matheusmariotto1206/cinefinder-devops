server:
  port: ${PORT:8080}

apex:
  top-movies-url: https://oracleapex.com/ords/cinefinder/cinefinder/top-movies

spring:
  application:
    name: cinefinder-java-advanced

  datasource:
    url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
    username: rm559369
    password: 121104
    driver-class-name: oracle.jdbc.OracleDriver


  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/oracle

#  profiles:
#    active: dev

---
spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:mem:cinefinder;MODE=Oracle
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration/h2